A Google Chrome extension that allows a user to queue YouTube videos. A user can right-click a video and select "add to list" or simply drag the video to the queue window, which pops up when a video is dragged. The queue window is also toggled by clicking the "Next" icon in the extension bar. 
